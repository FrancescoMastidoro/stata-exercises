# Feedback: Day 3

**Overall**: Strong solution with correct commands throughout; Task 2 and 3 are conflated but the required analysis is present, and the bonus shows excellent initiative with two valid methods.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Load and inspect | PASS | Correct use of `sysuse census` and `describe`; observations, unit of observation, and variable types all correctly identified. |
| 2. Summarize population | PARTIAL | Used `summarize pop, detail` instead of plain `summarize pop` as asked — technically gives more than requested. More importantly, the task asks to note mean/min/max and discuss why the mean might be misleading; the mean and median discussion is there but min/max are not explicitly recorded in comments. The core reasoning about the median being more representative is correct. |
| 3. Detailed distribution | PARTIAL | The actual command `summarize pop, detail` was run in Task 2 rather than here, and Task 3 just says "see answer to point 2." While the analysis is correct and present, it is misplaced — the task explicitly asks for the `detail` option here and a comparison of median vs mean with a conclusion about skew direction. The content is right but the structure is sloppy. |
| 4. Tabulate + summarize by region | PASS | `tabulate region` run first, then `summarize pop if region == 1/2` correctly used. The comparison between NE and N Cntrl is correct. The `bysort` addition is a nice bonus. |
| Bonus | PASS | Two valid methods implemented: manual `r(N)*r(mean)` approach and `tabulate region [fweight=pop]`. The weighted tabulate is exactly the idiomatic Stata solution. Interpretation is correct — South has the largest population share. |

## What to work on

Tasks 2 and 3 were effectively merged into one block, which caused Task 3 to have no executable code of its own. In future, follow the task numbering strictly — run the plain `summarize pop` in Task 2 as instructed, then re-run with `, detail` in Task 3; this makes your do-file easier to follow and ensures each task is self-contained. Also get into the habit of explicitly pulling and commenting on the specific numbers the task asks for (min, max here) rather than selectively reporting only the ones that support your narrative.