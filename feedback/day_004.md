# Feedback: Day 4

**Overall**: Strong effort across all tasks with correct commands and good interpretation; one skew direction error in Task 3 and a minor gap in Task 4.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Load and inspect | PASS | `sysuse nlsw88` (without `, clear`) works fine if no dataset is loaded, and `describe` is correct. Two labeled numeric variables are correctly identified (occupation, industry) with sensible real-world descriptions. |
| 2. Summarize hours | PASS | `summarize hours` is correct, values are recorded accurately, and the comment on the minimum is reasonable. |
| 3. Detailed distribution | PARTIAL | `summarize hours, detail` is correct and the median/mean comparison is right, but the skew direction is wrong. If median (40) > mean (37.2), the distribution is **left-skewed** — Francesco states this correctly in words but then gives a contradictory interpretation: left skew implies a tail toward low values, which actually *is* consistent with part-time workers. The logic is tangled, however, because Francesco initially writes "median > mean → left-skewed" correctly, then the follow-up `count if inlist(hours, 1, 3)` checks only two specific values rather than a meaningful part-time threshold. The core directional claim is correct but the reasoning is muddled. |
| 4. Tabulate and compare groups | PARTIAL | `tabulate race` and both `summarize wage if race == 1/2` commands are correct. The gap relative to overall mean is described loosely ("0.3 dollars more / 1 dollar less") without actually running `summarize wage` to get the overall mean first — it appears to be eyeballed rather than computed explicitly in the do-file. |
| Bonus | PASS | `tabulate occupation, sort` correctly identifies top occupations; numeric codes are verified with `nolabel`; `summarize wage if occupation == X, detail` is used correctly for all three; IQR and median are read and compared; interpretation is sensible. |

## What to work on

**Task 3**: Be precise about skewness language. Left skew (negative skew) means the *left* tail is longer — i.e., a few workers with very low hours pull the mean down. This is consistent with part-time workers, but make sure the verbal explanation matches the direction rather than mixing signals. Practice stating: "mean < median → left skew → long tail on the left."

**Task 4**: Always compute the overall mean explicitly in your do-file with `summarize wage` before making comparative statements — do not eyeball gaps from memory. One extra line of code makes the claim verifiable and reproducible.