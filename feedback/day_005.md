# Feedback: Day 5

**Overall**: Strong effort with correct commands throughout; minor issues in task 4 (region label misreading and missing `if`-based comparisons) and a small typo in task 2 commentary, but the core Stata syntax is sound.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Load and inspect | PASS | Correct commands and observations. `region` is correctly identified as a labeled numeric (though not explicitly called out as labeled — it is listed under "numeric" without distinguishing it from unlabeled ones). Country as string is correct. Variable concepts are noted. Minor omission: `safewater` concept not described, but the key variables are covered. |
| 2. Summarize continuous variable | PASS | Correct command, correct statistics recorded. The sentence of interpretation is present and reasonable. Small typo ("welk-known") but this is a comment, not code. |
| 3. Detailed distribution | PARTIAL | The command is correct. The commentary conflates two things: it first says the distribution is "rather symmetric" because mean ≈ median, then pivots to calling the skewness "strong" based on the skewness statistic. The skewness statistic of −0.908 is meaningful but the claim about quartile distances is stated incorrectly — the comment says "distance between third and fourth quartile" (there is no fourth quartile; quartiles define three cut points). The interpretation of most countries clustering near the top is correct directionally but the reasoning is muddled. |
| 4. Tabulate and compare groups | PARTIAL | `tabulate region` and `bysort region: summarize lexp` are both valid and produce the right output. However, the task explicitly asks for `summarize lexp` with an `if` condition for at least two specific regions, and the hint specifically calls this out. Using `bysort` is a reasonable alternative but the label reading is also wrong: the dataset has only three regions (Europe & Central Asia, North America, South America) — calling one "North America" with 14 obs and another "South America" with 10 obs may misidentify actual labels (the dataset uses "Europe & C. Asia", "North America", "South America"). More critically, no explicit two-region gap comparison is stated — the comment only compares Europe & Central Asia to the overall mean, not to another region directly. |
| Bonus | PASS | Correct use of `summarize gnppc if region == 1, detail` and unrestricted version. Median and IQR figures are read and compared correctly. Interpretation is clear and appropriate. |

## What to work on

For **Task 3**, be precise with statistical vocabulary: quartiles are cut points (Q1, Q2, Q3), not intervals labelled "first through fourth" — rephrase as "the distance between Q1 and Q2 is larger than between Q2 and Q3." Reconcile your narrative too: if the skewness statistic is −0.908 you should lead with "moderately to strongly left-skewed" rather than first claiming near-symmetry.

For **Task 4**, follow the hint's instruction and write explicit `summarize lexp if region == #` commands for at least two regions so the comparison is transparent and reproducible; `bysort` gives the same numbers but buries the two-region gap the task asks you to compute and comment on directly.