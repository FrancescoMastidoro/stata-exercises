# Feedback: Day 6

**Overall**: Good effort with mostly correct code, but Task 1 has a significant classification error and Task 4 modifies the data in place rather than proposing filter conditions as instructed.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Audit variable types | PARTIAL | The classification of labeled vs. unlabeled variables is largely inverted. In `nlsw88`, variables like `race`, `married`, `collgrad`, `south`, `smsa`, `c_city`, `industry`, `occupation`, and `union` carry value labels, while `idcode`, `age`, `grade`, `ttl_exp`, `tenure`, `wage`, `hours` are numeric without labels. The comment about `if` conditions is correct in spirit but underdeveloped — the key point is that with labeled numerics you must filter using the integer code (e.g., `if race == 2`), not the label string. |
| 2. Count and locate missing values | PASS | Correct identification of `union` as the most-missing variable, correct count (368), correct percentage computation, and a plausible real-world explanation. `codebook` and `count if missing()` are used appropriately. |
| 3. Flag and examine missing observations | PASS | `generate flag_miss = missing(union)` is correct syntax, `tabulate flag_miss` confirms counts, and separate `summarize wage` calls with `if` conditions are correct. The interpretation noting higher mean wages among those with missing union status is reasonable. |
| 4. Impossible/suspicious values | PARTIAL | The inspection commands (`summarize hours, detail` and `summarize wage, detail`) are correct. However, the exercise asks to *propose* a rule expressed as an inequality — instead, Francesco executes `replace` commands that permanently alter the data, which is destructive and goes beyond what was asked. The chosen thresholds are also questionable: excluding `hours < 15` removes part-time workers who are entirely plausible in this dataset, and the exercise asks for the *most suspicious* extreme values, not broad trimming. |
| Bonus | PASS | `tabulate occupation flag_miss, row` is correct, the overall missing rate reference is used properly, and the comment identifies specific occupations with elevated missingness with a reasonable analytical note. |

## What to work on

**Task 1**: Run `describe` on `nlsw88` and carefully read the "value label" column — a non-empty entry in that column means the variable is labeled. Re-do the classification from scratch; you have the labeled and unlabeled groups nearly swapped. **Task 4**: When an exercise says "propose a rule expressed as an inequality," write it as a comment (e.g., `* exclude if hours > 80`) or at most as a passive `if` filter on a `summarize` — do not `replace` values in a data-auditing script, as that silently changes the dataset for all downstream tasks and makes your later `summarize` results misleading.