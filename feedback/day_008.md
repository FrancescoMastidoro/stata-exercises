# Feedback: Day 8

**Overall**: Good structure and effort, but the solution contains several factual errors about the dataset that undermine the analysis — most critically, `wage` has no missing values in `nlsw88`, which Francesco correctly discovers but then mis-states in Task 2, and the min/max values for `hours` are swapped in Task 4.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Load and audit missingness | PARTIAL | Commands are correct. However, the comment claims `union` has the highest missing count (368) — this is correct per the dataset reference. The claim that "six variables contain missing values" is also correct. No major errors here, so this is close to a pass, but note the bonus `describe` later creates variables (`flag_miss`, `flag_problem`) that don't exist in the original dataset, suggesting some confusion. |
| 2. Count missings manually | PARTIAL | The `count` commands are syntactically correct and the conclusion ("they add up") is logically sound. However, the comment says "0 observations" have missing wages — this is actually correct per the dataset reference (`wage` has no missing values), but it contradicts the setup of Task 3, and Francesco appears confused rather than intentionally noting this. The insight that wage has zero missings should have been flagged more clearly as a surprising finding worth remarking on. |
| 3. Flag and examine missing wages | PARTIAL | `gen missing_wage = missing(wage)` is correct syntax. `tabulate missing_wage` and `bysort missing_wage: summarize hours` are correct commands. The conclusion that the question cannot be answered because wage has no missings is logically valid given the data. However, `missing_wage` will always equal 0 for every observation, making the `bysort` comparison meaningless — Francesco should have noted this more explicitly and ideally flagged `union` or `hours` as better candidates to demonstrate the technique. |
| 4. Detect impossible values | PARTIAL | `summarize hours, detail` and the `count` commands are syntactically correct. However, the min and max are **swapped** in the comment: Francesco writes "minimum: 80; maximum: 1" — it should be minimum: 1, maximum: 80. The `count if hours > 80` correctly returns 0 (no values exceed 80), but the comment about the minimum/maximum is factually wrong and suggests the output was not read carefully. |
| Bonus | PASS | Commands are correct: `describe`, `tabulate race`, `numlabel, add`, and a second `tabulate race`. The explanation of why numeric codes alongside labels are useful for `if` conditions is accurate and well-reasoned. The variable type list has minor issues (lists generated variables `flag_miss`, `flag_problem`, `missing_wage` as if they were original) but the core task is done correctly. |

## What to work on

In Task 4, always double-check that your comments reflect what Stata actually printed — writing "minimum: 80; maximum: 1" is the opposite of the true values (min 1, max 80) and suggests you may have read the output in the wrong direction or copied numbers incorrectly. For Task 3, when you discover that the variable you were asked to flag has *zero* missing values, treat that as a finding worth highlighting explicitly and consider demonstrating the technique on a variable that does have missings (e.g., `union` or `hours`) so the exercise still has analytical value. Finally, avoid listing variables in comments (Task 5 type list) that you generated yourself mid-do-file as if they were original dataset variables — it creates confusion about the dataset's actual structure.