# Feedback: Day 10

**Overall**: Strong solution with correct commands throughout; minor conceptual errors in comments on rep78's meaning and the skewness description, but the analytical instinct and code quality are genuinely good.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Variable types and structure | PASS | Correctly identifies `make` as the string variable and `foreign` as a labeled numeric type. The comment on string inconsistency problems is valid. One gap: the exercise asks to identify *which variables* are labeled numeric — `rep78` is also labeled numeric and is not mentioned here, but the core task is executed correctly. |
| 2. Missing values | PARTIAL | Correctly uses `misstable summarize` and `count if missing(rep78)`, confirms 5 missing observations. However, the substantive comment is incorrect: missing repair records most plausibly reflect cars that were *too new* (foreign imports) or had no recorded repairs, not that they were faulty or repaired many times. This is a meaningful conceptual error. |
| 3. Distribution of rep78 | PARTIAL | `summarize rep78, detail` and `codebook rep78` are correct. The `di _N - r(N)` trick is clever and works. However, the comment that rep78 "reports the number of repairs recorded in 1978" is wrong — it is a 1–5 *quality rating* scale (repair record rating), not a count of repairs. This is a factual error verifiable via `codebook`. The skewness comment (mean > median → right skew) is directionally correct but overstated given the tiny difference. |
| 4. Implausible values in mpg | PASS | Correctly retrieves min (12) and max (41) consistent with the dataset reference. Uses `tabulate mpg`, `count if mpg < 15`, and `count if mpg > 35`. The additional `list` command cross-checking weight and length is excellent analytical practice and the conclusions drawn are well-reasoned. |
| Bonus | PASS | Correctly uses `tabulate foreign` with and without `nolabel` to confirm codes. Runs conditional `summarize` for both groups. Critically, uses `tab foreign if missing(rep78)` to identify that 4 of 5 missing values are domestic — a sharp observation that directly addresses the exercise prompt. |

## What to work on

The most important correction is Task 3: `rep78` is a **1–5 quality rating** (1 = worst, 5 = best repair record), not a count of repairs — run `codebook rep78` and read the value labels carefully. On Task 2, the missing-value interpretation should be grounded in what the variable actually measures: cars with missing repair ratings are likely ones with no repair history to rate (e.g., new foreign imports), not cars that were broken frequently. Get in the habit of reading `codebook` output fully before writing substantive comments, as the label descriptions contain exactly this information.