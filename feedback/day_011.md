# Feedback: Day 11

**Overall**: All four required tasks are completed with correct syntax and reasonable comments, though Task 2 has axis labels swapped.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Histogram of median age | PASS | Correct syntax with `frequency` and `title()`. The comment about center (~30) and symmetric shape is reasonable given the medage range of 24.2–34.7. |
| 2. Scatter plot of urbanization and median age | PARTIAL | The axis labels are swapped: `xtitle()` receives "Median Age" but the x-axis is `popurban000`, and `ytitle()` receives "Urban population (thousands)" but the y-axis is `medage`. The variables themselves are plotted correctly (`medage` on y, `popurban000` on x), so only the titles are wrong. |
| 3. Overlay linear fit | PARTIAL | Same axis label swap carried over from Task 2. The `||` syntax for combining plot types is valid in Stata and will work correctly, and the comment is appropriate. |
| 4. Bar chart by region | PASS | Correct command, title, and ytitle. Identifying the Northeast (NE) as the highest average median age region is consistent with the dataset. The comment is brief but adequate. |
| Bonus | PASS | Valid use of `by(region)` inside `histogram`. The comment comparing regional distributions is reasonable, though calling "N cntrl" tighter than others is a defensible observation. |

## What to work on

In Task 2 (and consequently Task 3), the `xtitle()` and `ytitle()` strings are assigned to the wrong axes — `xtitle()` should describe what is on the x-axis (`popurban000`, i.e., urban population) and `ytitle()` should describe the y-axis (`medage`). Always cross-check each title option against the variable order in the plot command: the first variable listed after `scatter` is y, the second is x. Fix this swap in both tasks.