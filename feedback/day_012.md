I notice the solution file appears to contain only a header comment and no actual Stata code. The `.do` file body is empty (or was not included in the submission). I'll evaluate based on what was provided.

# Feedback: Day 12

**Overall**: No executable Stata code was submitted — the file contains only a title comment and the dataset reference block, with no actual commands for any of the five tasks.

## Task-by-task

| Task | Status | Comment |
|------|--------|---------|
| 1. Wage histogram | FAIL | No `histogram` command present — neither the basic version nor the one with `normal` option was written. |
| 2. Histogram by race | FAIL | No `histogram wage, by(race)` command present. |
| 3. Scatter + lfit | FAIL | No `twoway scatter` or `lfit` commands present. |
| 4. Bar chart by occupation | FAIL | No `graph bar` command present. |
| Bonus | SKIP | No code submitted. |

## What to work on
The submission is entirely missing — you need to actually write Stata commands, not just a header. Start with Task 1: `sysuse nlsw88` followed by `histogram wage, title("Wage Distribution") xtitle("Wage")`, then add `, normal` to the same command on a second attempt. For Task 3, practice the `twoway (scatter wage ttl_exp) (lfit wage ttl_exp)` syntax specifically, as combining plot types in a single `twoway` call is a skill that will recur throughout the series. Do not submit a `.do` file that contains only comments — every task needs at least one executable line of code.